<Window x:Class="HyundaiTransys.VisionInspection.UI.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Administrator login"
        SizeToContent="WidthAndHeight"
        MinWidth="360" MinHeight="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="24" VerticalAlignment="Center">
        <Label Content="User"/>
        <TextBox x:Name="txtUsername" Text="{Binding UserName, UpdateSourceTrigger=PropertyChanged}"/>          <Label Content="Password" Margin="0,12,0,0"/>
        <PasswordBox x:Name="txtPassword" PasswordChanged="Pwd_Changed"/>
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Margin="0,8,0,0"/>
        <Button Content="Login" Click="LoginButton_Click" HorizontalAlignment="Right" Margin="0,16,0,0" Padding="24,6"/>
    </StackPanel>
</Window>

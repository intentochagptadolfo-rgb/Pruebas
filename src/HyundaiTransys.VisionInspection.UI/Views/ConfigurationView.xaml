<Window x:Class="HyundaiTransys.VisionInspection.UI.Views.ConfigurationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Administrator — Configuration"
        Width="1100" Height="720"
        WindowStartupLocation="CenterOwner">
    <TabControl Margin="12">
        <TabItem Header="MES">
            <StackPanel Margin="16">
                <Label Content="Role (Server/Client)"/>
                <ComboBox SelectedItem="{Binding Settings.Mes.Role}">
                    <sys:String xmlns:sys="clr-namespace:System;assembly=System.Runtime">Server</sys:String>
                    <sys:String xmlns:sys="clr-namespace:System;assembly=System.Runtime">Client</sys:String>
                </ComboBox>
                <Label Content="IP address"/>
                <TextBox Text="{Binding Settings.Mes.IpAddress}"/>
                <Label Content="Port"/>
                <TextBox Text="{Binding Settings.Mes.Port}"/>
                <Label Content="Read timeout (ms)"/>
                <TextBox Text="{Binding Settings.Mes.ReadTimeoutMs}"/>
                <Label Content="Reconnect max delay (ms)"/>
                <TextBox Text="{Binding Settings.Mes.ReconnectMaxDelayMs}"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Keyence camera">
            <StackPanel Margin="16">
                <Label Content="IP address"/>
                <TextBox Text="{Binding Settings.Keyence.IpAddress}"/>
                <Label Content="Port"/>
                <TextBox Text="{Binding Settings.Keyence.Port}"/>
                <Label Content="Command timeout (ms)"/>
                <TextBox Text="{Binding Settings.Keyence.CommandTimeoutMs}"/>
                <Label Content="Result timeout (ms)"/>
                <TextBox Text="{Binding Settings.Keyence.ResultTimeoutMs}"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Storage">
            <StackPanel Margin="16">
                <Label Content="OK images folder"/>
                <TextBox Text="{Binding Settings.Storage.OkImagesPath}"/>
                <Label Content="NG images folder"/>
                <TextBox Text="{Binding Settings.Storage.NgImagesPath}"/>
                <Label Content="Retention (days)"/>
                <TextBox Text="{Binding Settings.Storage.RetentionDays}"/>
                <Label Content="Max disk usage (GB)"/>
                <TextBox Text="{Binding Settings.Storage.MaxDiskUsageGb}"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Database">
            <StackPanel Margin="16">
                <Label Content="Provider (Sqlite / PostgreSql)"/>
                <TextBox Text="{Binding Settings.Database.Provider}"/>
                <Label Content="Connection string"/>
                <TextBox Text="{Binding Settings.Database.ConnectionString}"/>
            </StackPanel>
        </TabItem>

        <TabItem Header="Users">
            <DockPanel Margin="16">
                <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBox Width="180" Text="{Binding NewUserName}" ToolTip="user name"/>
                    <PasswordBox Width="180" Margin="8,0"/>
                    <ComboBox Width="140" Margin="8,0"
                              ItemsSource="{Binding Source={x:Static local:UserRoleValues.All}}"
                              SelectedItem="{Binding NewUserRole}"
                              xmlns:local="clr-namespace:HyundaiTransys.VisionInspection.UI.ViewModels"/>
                    <Button Content="Add" Padding="16,4" Command="{Binding AddUserCommand}"/>
                </StackPanel>
                <DataGrid ItemsSource="{Binding Users}" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="User" Binding="{Binding UserName}" Width="*"/>
                        <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="120"/>
                        <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80"/>
                        <DataGridTextColumn Header="Last login" Binding="{Binding LastLoginAt}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </TabItem>

        <TabItem Header="JOB mapping (truth table)">
            <DataGrid ItemsSource="{Binding JobMappings}" AutoGenerateColumns="False" Margin="16">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Model" Binding="{Binding ModelCode}" Width="*"/>
                    <DataGridTextColumn Header="Variant" Binding="{Binding VariantKey}" Width="*"/>
                    <DataGridTextColumn Header="JOB" Binding="{Binding JobId}" Width="80"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="2*"/>
                    <DataGridCheckBoxColumn Header="Enabled" Binding="{Binding IsEnabled}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </TabItem>

        <TabItem Header="Save">
            <StackPanel Margin="16">
                <Button Content="Save configuration"
                        Command="{Binding SaveCommand}"
                        Height="48" Width="240" HorizontalAlignment="Left"/>
            </StackPanel>
        </TabItem>
    </TabControl>
</Window>
